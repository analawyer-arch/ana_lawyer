# HR FAQ Bot

Telegram-бот для сотрудников отдела кадров. Отвечает на типовые вопросы по 6 темам
и обрабатывает свободные вопросы через Anthropic Claude.

---

## Структура проекта

```
hr-faq-bot/
├── bot.py                     # Точка входа
├── handlers/
│   ├── menu.py                # Главное меню и навигация по темам
│   └── free_question.py       # Обработка свободных вопросов через AI
├── content/
│   ├── employment.md          # Трудоустройство по ТК
│   ├── vacation.md            # Отпуск
│   ├── sick_leave.md          # Больничный
│   ├── ip.md                  # ИП и самозанятые
│   ├── accreditation.md       # IT-аккредитация
│   └── military_deferment.md  # Отсрочка от армии
├── logs/
│   └── unanswered.log         # Вопросы без ответа (создаётся автоматически)
├── .env.example               # Шаблон переменных окружения
├── requirements.txt
└── README.md
```

---

## Быстрый старт

### 1. Клонируйте / скачайте проект

```bash
cd hr-faq-bot
```

### 2. Создайте виртуальное окружение и установите зависимости

```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# Linux / macOS
source .venv/bin/activate

pip install -r requirements.txt
```

### 3. Создайте файл `.env` из шаблона

```bash
# Windows
copy .env.example .env

# Linux / macOS
cp .env.example .env
```

Откройте `.env` и заполните значения:

| Переменная          | Описание                                                       |
|---------------------|----------------------------------------------------------------|
| `BOT_TOKEN`         | Токен бота от [@BotFather](https://t.me/BotFather)             |
| `ANTHROPIC_API_KEY` | API-ключ с [console.anthropic.com](https://console.anthropic.com/) |
| `HR_CONTACT`        | Контакт специалиста кадров (например, `@hr_manager` или email) |

### 4. Заполните контентные файлы

Откройте файлы в папке `content/` и замените строку `[Текст ответа будет добавлен]`
на реальные ответы по каждой теме. Файлы поддерживают обычный текст.

### 5. Запустите бота

```bash
python bot.py
```

Бот начнёт принимать сообщения. Для остановки нажмите `Ctrl+C`.

---

## Логирование неотвеченных вопросов

Если Claude не смог уверенно ответить на вопрос, бот:

1. Отправляет пользователю стандартный ответ с контактом специалиста.
2. Записывает вопрос в `logs/unanswered.log` в формате JSON-строк:

```json
{"date": "2026-06-15T12:00:00", "username": "ivan_petrov", "user_id": 123456789, "question": "..."}
```

---

## Как работает ИИ-ответ

При нажатии «💬 Задать свой вопрос» бот:

1. Переходит в режим ожидания текста.
2. Отправляет вопрос в Anthropic API (модель `claude-sonnet-4-6`) вместе
   со всем содержимым папки `content/` в качестве базы знаний.
3. Если Claude отвечает `UNKNOWN` — вопрос логируется, пользователь получает
   направление к специалисту.
4. Иначе — ответ отправляется пользователю.

---

## Требования

- Python 3.11+
- Доступ к Telegram Bot API и Anthropic API
